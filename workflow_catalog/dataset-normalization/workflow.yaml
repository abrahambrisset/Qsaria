slug: dataset-normalization
title: Dataset normalization
summary: Load uploaded or session tabular data, normalize it for analysis, and prepare it for GTM or reporting.
status: stable
tags:
  - dataset
  - pandas
  - normalization
keywords:
  - normalize
  - normalise
  - standardize dataset
  - uploaded
  - normalization
preflight_tools:
required_tools:
  - pandas_normalize_for_analysis
optional_tools:
  - pandas_load_dataframe_from_session
  - pandas_create_dataframe
  - pandas_run_operation
  - session_list_loadable_session_data
  - session_summarize_session_memory
expected_artifacts:
  - normalized_dataset_path
  - analysis_ready_dataframe
recommended_prompt: chemoinformatician_agent
